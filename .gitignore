node_modules
dist
dev-dist
*.local
.DS_Store
*.tsbuildinfo

# Backend store (created at runtime)
server/*.db
server/*.db-shm
server/*.db-wal

# Backend deployment settings (per-host)
server/deploy.env
server/data/
